import { TitleProps } from '@/component/ProTable/types';

const Title = ({ title }: TitleProps) => {
  // state
  // function
  // useEffect
  return <h2 className={'mb-2 text-lg font-bold'}>{title}</h2>;
};

export default Title;
